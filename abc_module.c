/*#include<stdio.h>
#include <stdlib.h>
#include<GL/glut.h>


extern int stage,n,no,frame[10],yb[100],choice,stagel;

void abc()
{		
		
     	if(choice == 1)                          //after every left click next stage will be execute   
		{	
			if(stage > n-1){
			        	printf("\nprocess terminated\n");
				
				//exit(0);
		 }
		 else{
			compute();
			fcfs(stage);
			}
		}

		else if(choice == 2)
		{
			compute();
			lru(stagel);
		}
//	glFlush();
}
*/
