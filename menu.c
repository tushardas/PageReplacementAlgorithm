#include <GL/glut.h>

void menu()
{

    /*char menus[][30] = {{"1.First In First Out"}, {"2.Least Recently Used"},{"3.Optimal Page Replacement"}};

    glColor3f(0.0,0.0,0.0);
    gprint(20,300,0,menus[0]);
    gprint(20,250,0,menus[1]);
    gprint(20,200,0,menus[2]);
    */


}